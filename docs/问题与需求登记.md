# 问题与需求登记（mark-readnotes）

格式：现象 / 根因 / 修法 / 证据 / 状态。新的往下加。

## P1 新建块时标签被粘成 `unsortedweb`（已修）

- **现象**：新建块时标签框预填 `unsorted`，用户直接输入 `web` 得到 `unsortedweb`，块被写进 `unsortedweb.md`。
- **根因**：`openBoard(null)` 用 `fillBoard('', DEFAULT_TAG, '')` 预填默认标签，短字段又不全选，输入等于在默认值后面追加。
- **修法**：新块标签预填「上次用过的标签」，没有就留空；标签框聚焦即全选（`focus → select()`）。
- **证据**：`docs/验收清单-第1轮.md` F5。
- **状态**：已修并复验。

## P2 返回键“没反应”的读数存疑（已澄清，非缺陷）

- **现象**：第一次测「按返回键收白板」时读到 `boardOpen=true`。
- **排查**：那一轮前一个动作刚把链接交给系统浏览器，前台曾被打断；当时没有日志可判断是否进到 `onBackPressed`。
  加 `back.enter` / `back.js=` 日志后清干净前台重测 → `back.js="1" handled=true finished=false`，白板关闭、进程不退出。
- **状态**：已澄清（原读数归因于当时前台环境）。

## P3 SAF 不能选存储根目录（设计已适配）

- **现象**：`ACTION_OPEN_DOCUMENT_TREE` 停在内置存储根，系统显示 "Can't use this folder"。
- **处置**：引导用户选一个子目录（如 Documents），应用在其中**自建** `mark-readnotes`（目录名固定英文）。
- **状态**：已按此设计实现（第 1 轮 F1）。

## P4 模拟器 AVD `test34` 起不来（环境问题）

- **现象**：容器 7 秒 `Exited (1)`，日志 `Failed to process .ini file …/test34.avd/config.ini` + `FATAL | CPU Architecture 'arm' is not supported`。
- **根因**：`test34.avd/` 下缺 `config.ini`，模拟器落到默认（arm）配置。
- **处置**：改用 `test35`（x86_64 / android-34）。已写进 skill。
- **状态**：绕过。

## P5 http 图片被浏览器拦掉，且 compat 模式也不放行（已修，含两个取舍）

- **现象**：`![网络图](http://10.0.2.2:8931/net.png)` 在预览里 `ok:false, 0x0`，本机 HTTP 服务一条请求都没收到。
- **真因**：壳子页面原本是 `https://appassets.androidplatform.net`，其上的 http 图片属于混合内容，
  Chrome 对**图片**按“可拦截”处理（自动升级失败即拦）。页面控制台原话：
  `Mixed Content: … requested an insecure image 'http://…'. This request has been blocked; the content must be served over HTTPS.`
  —— 把 `mixedContentMode` 改成 `MIXED_CONTENT_COMPATIBILITY_MODE` 也**不放行**（实测仍拦）。
- **修法（两个显式取舍）**：
  1. 壳子改走 **http 虚拟源**（`http://appassets.androidplatform.net`，仍是本地拦截、不出网）→ 不再存在混合内容；
     代价：页面不是安全源（本项目不用 `crypto.subtle`/service worker 这类需要安全源的能力）。
  2. 清单加 `android:usesCleartextTraffic="true"`（否则 Android 9+ 明文请求直接被平台拦）。
- **证据**：`docs/验收清单-第2轮.md` F10（`ok:true, 64×64` + 服务端日志 `GET /net.png 200`）。
- **状态**：已修并复验。

## P6 白板把小窗盖住，「删除本块」点不到（已修）

- **现象**：在白板里点「⋯」→ 小窗出现但点「删除本块」毫无反应（按钮文字也不变确认态）。
- **真因**：`#sheet` z-index 50 < `#board` z-index 60 —— 全屏白板盖在小窗之上，
  手指落在白板那一层（`elementFromPoint` 给的是白板的元素），小窗里的键根本没收到点击。
- **修法**：`#sheet` 提到 70（它就是从白板里点开的，必须在上层）。
- **证据**：`sheetZ=70 / boardZ=60`，`elementFromPoint(206,228) → BUTTON|删除本块`；
  两次点击后块从卡片列表与 md 文件里消失。见 `docs/验收清单-第2轮.md` F12。
- **状态**：已修并复验。

## P7 我自己把两个源文件写空了（代码事故，已恢复）

- **现象**：改版本号的脚本写成 `open(p,'w').write(open(p,'r').read().replace(...))` ——
  `open(p,'w')` 先执行就把文件**截断**了，参数再读回空串，于是 `MainActivity.kt` 与 `index.html` 变成 0 字节。
  构建照样“成功”（清单里的类名不会在编译期校验），装上去一启动就崩：
  `ClassNotFoundException: Didn't find class "dev.markreadnotes.MainActivity"`。
- **修法**：从 git 取回这两个文件（上一次提交的版本），再用逐条 patch 重新打上第 2 轮的改动；
  构建后**先证明构件里有这个类**（`unzip -p apk classes3.dex | strings | grep -c Ldev/markreadnotes/MainActivity;`）再安装。
- **教训**：批量改文件一律走 patch 工具或「先读进变量、再单独写回」，绝不把读写塞进同一个表达式；
  启动崩先看 `AndroidRuntime` 的 `ClassNotFoundException`，那是“包不对”而不是“代码逻辑错”。
- **证据**：恢复后 `MainActivity.kt` 202 行 / `index.html` 59 行；r4 构件启动日志
  `app.start build=0.1.0+r4 scan=skipped(reason=startup)`，无崩溃。
- **状态**：已恢复并复验。

## P8 干净重建“内容一致、整包 sha256 不一致”（不是缺陷，是打包差异，已写清口径）

- **做法**：把 git 里的源码快照导到干净目录重建（`bash tools/verify-repro.sh`），与归档构件对比。
- **实测**：`BUILD SUCCESSFUL`；两次产物的 **69 个条目逐条 CRC/原始大小/压缩大小完全一致**，
  但**整包 sha256 不同**（归档件 1,682,853 字节 vs 重建件 1,634,877 字节）。
- **查到的原因**：差异不在条目内容，而在 zip 的打包期结构 —— 首个不同字节出现在某个 local file header 的
  extra 字段长度（16 vs 20），随后每条的偏移被对齐填充带偏，累计差约 48KB。
- **口径（写进验收与 README）**：整包 sha256 只用来标识“交付的就是这一份”；
  **“源码可复现”用条目级对比来证**（`tools/verify-repro.sh` 会直接打出“内容不一致条目: 0”）。
  不把“哈希一致”当复现门槛，也不在没有证据的情况下声称可复现。
- **状态**：口径已定，脚本已归档（`tools/verify-repro.sh`）。

## P9 导入的冲突规则还没定（未开工，先登记）

- 用户原话：「根据哪个标签来导出文件，就根据哪个标签来导入文件，标签又不变」。
- 规则部分是清楚的（同一套：第 1 个标签＝一级目录、第 2 个＝二级目录；导入后块回到标签对应的 md 文件里，标签不动）；
  **不清楚的是**：导入时遇到笔记里已经存在的同名/同标题块怎么办 —— 覆盖 / 并列并存并对新块加后缀 / 跳过。
- 处置：下一轮开工前拿具体选项问用户，别猜（这条会改数据，猜错就是丢笔记）。
- **状态**：待用户决定。

## P10 分板块验收脚本自己的四个假红来源（都修了，留着给下次）

- **PATH 里少了 node**（`~/.local/bin`）：探针变成空值 → 一片「没落盘/没渲染」的假红。修：`_lib.sh` 里显式补 PATH。
- **点链接/系统分享把应用挤到后台**：2GB 模拟器会把它杀掉，之后探针 `fetch` 直接失败（一堆 undici 报错），
  看着像功能坏了。修：`refocus_app()`（退到前台 + 关掉被拉起来的 Chrome + 重挂 devtools 转发），每个板块开头都跑。
- **浮层里滚出屏幕的键**：按元素矩形点会 `Position out of bounds`。修：探针 `tap` 先 `scrollIntoView({block:'center'})`
  再量坐标；仍在视口外就直接报错（不再静默跳过）。**顺带发现一个界面问题见 P11。**
- **字符串比较带 JSON 引号**：探针输出是 JSON，`textContent` 是 `"已删除"` 带引号，直接比会假红。修：`unq()`。
- 另：基线要现取（本轮先建块 +1、删除 −1，用开场基线比就会假红）→ 判据改成按文件判定。

## P11 导出小窗里长预览会把动作键顶到折叠下方（待改）

- **现象**：文件树一长，「写入目录 / 系统分享」两个键被推到可滚动区域之外，得先滚动才能点到
  （探针因此报 `Position out of bounds`）。
- **处置（下一轮一起做）**：把动作键做成常驻（小窗底部固定一行），或在预览树上加高度上限。
- **状态**：仅界面改进，不影响功能（用手滚动一下就能点到）。

## P12 正文里打「一级标题」会把内容切成新块（需要在编辑时给提示）

- **现象**：验收脚本里在正文写 `# 一级` 当作示例，结果这一行之后的内容被拆成了**新的块**（文件里出现两个块）。
  这是「一级标题分块」规则的必然结果，不是 bug，但**用户在正文里打 H1 时不会有任何提示**。
- **处置（下一轮）**：白板里在正文行首输入 `# ` 时给一行提示（“这会把内容切成新块”），或提供“插入二级标题”按钮。
- **状态**：待做（已进 `docs/缺口清单.md`）。

## P13 基本 HTML 里的相对图片路径不走 /file/ 解析（待下一轮修）

- **现象**：验收块里放 `<img src="pic.png">`（原始 HTML）时，元素在，但 `naturalWidth=0`；
  同一段的 markdown 写法 `![本地图](pic.png)` 是好的（64px）。
- **真因**：`/file/<相对路径>` 的重写只做在 markdown 的图片语法上（`imgSrc()`），
  放行的原始 HTML 里的 `src` 原样保留 → 相对路径落到虚拟源根上 → 404。
- **处置（第 5 轮渲染重写时一起做）**：消毒阶段（白名单）顺手把保留元素上的相对 `src` 重写成
  `/file/…`；补一条断言：两种写法的图都必须 `naturalWidth>0`。
- **状态**：**第 5 轮已修**（消毒阶段对白名单里保留的 `img` 统一重写 `src`），判据 `boards/render.sh#R7 原始 HTML 图片已加载`（实测两种写法都是 64px）。

## P14 设备侧 `ls -R | grep` 会段错误（已改成宿主侧判定）

- **现象**：导出板块里查“有没有 `-2` 副本”的那条，输出里出现 `Segmentation fault`，计数拿到空值 → 假红。
- **处置**：改成先把目录清单 `cat` 回宿主，再用 python 计数（顺便也是更稳的中文判定方式）。
- **状态**：已修。

## P15 逐个 token 消毒会把成对的行内 HTML 拆散（第 5 轮开发中发现并修好）

- **现象**：`<span style="color:red">带颜色的 span</span>` 渲染成
  `<p><span style="color:red"></span>带颜色的 span</p>` —— span 变空、文字被挤到标签外面。
- **真因**：markdown-it 把行内 HTML 拆成两个 token（开始标签一个、结束标签一个）。
  用 `<template>` 逐个 token 解析时，浏览器会把没闭合的开始标签自动补成 `<span …></span>`，
  孤立的 `</span>` 又被丢掉 —— 标签对就散了。
- **修法**：行内 HTML 不在 token 层动；改成**整段一起解析**（游离容器）→ **整体消毒** → 再挂进页面。
  副产品：先消毒后上屏，危险的标签/属性在接触真实文档之前就已经没了。
- **回归判据**：`boards/render.sh` 的 `R5 行内 HTML 里的文字没被挤到标签外`（读 span 的 textContent）。
- **状态**：已修（渲染板块 29 绿 0 红）。

## P16 整理旧布局会把块在索引里变成两份（第 6 轮开发中发现并修好）

- **现象**：点「整理到随笔目录」后，块数从 8 变成 **15**（正好多一份）；文件内容本身没错。
- **真因**：搬家是「在新位置建文件 → 写 → 删旧文件」，新文件的 docUri 变了 → 索引里多出一个 `note_file` 行；
  旧行名下的块行既没改指也没标掉 → 下次扫描给新行重新入库一遍块，**旧行那份还留着**，于是重复。
- **修法**：搬家后 `repointBlocks(旧行 → 新行, 新 docUri)`（保住块的 id 与「最近编辑」顺序），再 `dropFile(旧行)`；
  另外给 `refresh` 补一条一致性收尾：文件不在了，它名下的块一起标成不在（不留孤儿块）。
- **回归判据**：`boards/store.sh` 的 `store.migrate 搬完块数没变（D3）`、`store.migrate 该文件的块都在（D3）`。
- **状态**：已修（存储板块 23 绿 0 红）。

## P17 分板块验收脚本又踩的三个坑（第 6 轮，都修了）

- **按文件名查落位，撞上一轮留下的同名旧行**：`idx.files` 里可能同时存在 `随笔/boardtest2.md`（旧）
  与 `随笔/网页随笔/boardtest2.md`，按 name 取 `[0]` 会拿到旧那行 → 假红。
  → 改成按**这个块的 id** 查它自己的 `relPath`（`blk.get`），位置随映射变也不会错。
- **只调 op 不刷页面状态**：`call("idx.refresh")` 之后没 `await reload()`，读 `ST.src.legacy` 还是旧值
  （「待整理」按钮看着没出现）→ 假红。→ 调完必须让页面把状态取回来。
- **验收残留没归位 / 复用同一个文件名**：上一轮被打断时留在根下或随笔目录里的同名文件，
  会触发「同名同内容只删旧」的分支，导致「块数不变」的假设不成立（产品行为是对的）。
  → 每组开头 `rm -f` 归位 + 用 `名字-$时间戳` 的唯一名。
- 另外两条这次的：`page` 回的是 JSON，字符串带引号要过 `unq`；`assert_true` 要传 True/False，不能直接塞计数。

## 需求（用户点过名、还没做）

- R9 导入（下一轮，先定冲突规则）、R3 WebDAV、R4 图片插入入口、R5 标签筛选与块内搜索、
  R6 其它功能板块、R7 外部控制口、R8 列表懒加载。明细见 `docs/缺口清单.md`。
